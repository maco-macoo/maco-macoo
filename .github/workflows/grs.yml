name: Update README cards

on:
  schedule:
    - cron: "0 0 * * *" # Runs once daily at midnight (UTC)
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - uses: actions/checkout@v6

      - name: Generate stats card (light)
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: stats
          options: show_icons=true&hide_border=true&bg_color=00000000&count_private=true
          path: profile/stats-light.svg
          token: ${{ secrets.PAT_1 }}

      - name: Generate stats card (dark)
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: stats
          options: show_icons=true&hide_border=true&bg_color=00000000&count_private=true&theme=github_dark
          path: profile/stats-dark.svg
          token: ${{ secrets.PAT_1 }}

      - name: Generate top languages card (light)
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: top-langs
          options: layout=compact&langs_count=8&hide_border=true&bg_color=00000000
          path: profile/top-langs-light.svg
          token: ${{ secrets.PAT_1 }}

      - name: Generate top languages card (dark)
        uses: stats-organization/github-readme-stats-action@v2
        with:
          card: top-langs
          options: layout=compact&langs_count=8&hide_border=true&bg_color=00000000&theme=github_dark
          path: profile/top-langs-dark.svg
          token: ${{ secrets.PAT_1 }}

      - name: Commit cards
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add profile/*.svg
          git commit -m "Update README cards" || exit 0
          git push
